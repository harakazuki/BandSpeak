<h3><%= @user.name %> さん</h3>

<h4>投稿一覧</h4>
<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %> - <%= post.created_at.strftime("%Y-%m-%d %H:%M") %></li>
    <p><%= post.body %></p>
  <% end %>
</ul>

<%= link_to '戻る', users_path %>