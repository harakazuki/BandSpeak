<h4>バンド一覧</h4>


<% @posts.each do |post| %>
  <tr>
  　<td>
  　　<%= link_to post.title, post %>
  　</td>
  　<td>
  　　<%= post.body %>
  　</td>
  
  
  
  　<td id="like_buttons_<%= post.id %>">
  　　<%= render "likes/like", post: post %>
  　</td>
　</tr>
<% end %>
