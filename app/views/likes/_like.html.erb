    <% if post.like_ids_by?(current_user) %>
    　<%= link_to post_likes_path(post), method: :delete, remote: true do %>  #remote: trueを追加
      ♡<%= post.likes.count %>いいね
    <% end %>
    <% else %>
      <%= link_to post_likes_path(post), method: :post, remote: true do %>  #remote: trueを追加
      ♡<%= post.likes.count %>いいね
    <% end %>
　　<% end %>